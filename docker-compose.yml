services:
  budget-bot:
    build:
      context: ./budget_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/budget.db
    volumes:
      - bot-data:/data

volumes:
  bot-data:
